<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:gravity="center"
        android:orientation="vertical"
        android:paddingHorizontal="25dp">

        <ImageView
            android:id="@+id/permissionIllustration"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:layout_margin="35dp"
            app:srcCompat="@drawable/notifications"
            app:tint="?attr/fg_accent" />

        <TextView
            android:id="@+id/permissionTitleTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_weight="0"
            android:fontFamily="sans-serif-medium"
            android:text="Allow us to send notifications"
            android:textAlignment="center"
            android:textColor="?attr/fg"
            android:textSize="24sp" />
    </LinearLayout>

    <TextView
        android:id="@+id/permissionExplainTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="25dp"
        android:text="This permission is needed to show the current song in the Quick Settings menu."
        android:textAlignment="center"
        android:textColor="?attr/fg"
        android:textSize="15sp" />
</LinearLayout>